import numpy as np


def softmax_loss_vectorized(W, X, Y, reg):
    """

    :param W:
    :param X:
    :param Y:
    :param reg:
    :return:
    """

    loss = 0.0
    dW = np.zeros_like(W)

    return loss, dW
